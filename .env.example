# Copy to .env.local and fill in. Never commit .env.local.
# DATABASE_URL lives in .env (Prisma 7's config loader reads .env, not
# .env.local — see prisma7.config.ts). Copy this block there too for local dev.

DATABASE_URL="postgresql://zeroid_dev:zeroid_dev_local_only@localhost:5432/zeroid_dev"

ANTHROPIC_API_KEY=

BREVO_API_KEY=
# No verified sender identity for Zeroid yet — see build-spec.md §14/README.
BREVO_FROM_EMAIL=
BREVO_FROM_NAME=Zeroid

# This account's Flutterwave credentials are shared across Mongozutu, Flux9,
# and Zeroid (same merchant account) — rotating requires updating all three.
FLUTTERWAVE_PUBLIC_KEY=
FLUTTERWAVE_SECRET_KEY=
FLUTTERWAVE_ENCRYPTION_KEY=

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# ../platform-services' knowledge/RAG service, reached through an
# authenticated nginx path on wa.lagosbusinessgroup.com (borrowed domain,
# see platform-services/docs/build-spec.md and the platform-services-project
# memory) -- NOT the raw Docker host port, which has no auth of its own.
# PLATFORM_KNOWLEDGE_BASE_URL defaults to that path if unset.
PLATFORM_KNOWLEDGE_BASE_URL=
PLATFORM_KNOWLEDGE_API_KEY=

# Prospecting -- Places API (New), Text Search. Needs a Google Cloud
# project with billing enabled (Google gives ~$200/mo free credit, which
# should cover this at MVP volume) and the Places API enabled on it.
GOOGLE_PLACES_API_KEY=

# Prospecting -- YouTube Data API v3. Same Google Cloud project as Places
# works: enable "YouTube Data API v3" on it and use its own API key (or
# reuse GOOGLE_PLACES_API_KEY's project, just enable this API too). Free
# quota (10,000 units/day) covers real usage at this stage.
YOUTUBE_API_KEY=

# Prospecting -- Etsy Open API v3. Register an app at
# https://www.etsy.com/developers/register -- the "Keystring" it gives you
# is this key. Free, no billing needed for the read-only endpoints used
# here. Real limitation: Etsy doesn't expose seller emails/phones through
# this API at all -- results are shop name + Etsy URL only.
ETSY_API_KEY=

# WhatsApp lead capture -- talks directly to Meta's Graph API, not through
# ../platform-services (see platform-services-project memory, 2026-09-18,
# for why that's not the shared gateway it was meant to be). Set this to
# any random string yourself and put the same value in Meta's App dashboard
# webhook config -- it's not issued by Meta, you invent it.
WHATSAPP_WEBHOOK_VERIFY_TOKEN=
# Meta App -> Basic Settings -> App Secret. Used to verify each webhook
# POST is really from Meta (X-Hub-Signature-256), not to send messages --
# that's the access token stored per-business in WhatsAppConnection.
# Needs a real Meta App connected to the WABA, which doesn't exist yet.
WHATSAPP_APP_SECRET=
# Defaults to v21.0 if unset (matches ../platform-services' own default).
WHATSAPP_GRAPH_VERSION=
